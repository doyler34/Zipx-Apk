# Copy this file to `.env` (in the same folder) and set a long random password.
# The `.env` file itself is git-ignored so the real secret is never committed.
#
# Generate a strong value on the VPS with:  openssl rand -hex 24
MEDIAFLOW_PASSWORD=replace-with-a-long-random-string
