# Copy this to `.env` in the same folder. The TMDB v3 key below is the one the
# app already ships (it's public in the app source), so it's fine to reuse.
# You can also make your own free key at https://www.themoviedb.org/settings/api
TMDB_API_KEY=d168cb7e62f9692894c20fdb039ae126
